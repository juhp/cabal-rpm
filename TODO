Bugs/regressions
- use UTC for date
- take a filepath arg tarball

Features
- bring back building
- recursive rpm building of missing depends
- installation of already packaged depends (cf yum-builddep)
- updating/diff'ing of spec files
- maybe packaging of stacks into one deployable rpm
- support mock builds
- filter Cabal from pkg deps (if no version bound)
- handle ghc_arches_with_ghci
- conventional --version output
- generate C deps

Packaging
- drop hscolour requires when all releases of ghc-rpm-macros require it
